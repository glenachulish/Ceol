Review and refactor the file $ARGUMENTS to reduce bloat and improve code quality.

Work through the following in order:
1. **Remove dead code** — delete unused imports, variables, and functions
2. **Consolidate duplication** — if the same logic appears more than once, extract it into a shared function
3. **Simplify conditionals** — flatten nested if/else where possible, use early returns
4. **Right-size functions** — if a function is doing more than one thing, split it
5. **Clean up unused routes or endpoints** — flag anything that appears to be scaffolding or placeholder code

Make the changes directly. After finishing, give a brief summary of what was removed or restructured.
