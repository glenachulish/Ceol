Add clear, useful inline comments to the file $ARGUMENTS.

Follow these rules:
1. Add a docstring or comment block at the top of each function explaining what it does, its parameters, and what it returns
2. Add inline comments on any logic that isn't immediately obvious
3. For routes/endpoints, note what request they handle and what they return
4. Do NOT comment the obvious — avoid things like `# increment counter` above `count += 1`
5. Keep comments concise — one clear sentence is better than three vague ones

Make the changes directly to the file.
