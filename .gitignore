.venv/
__pycache__/
*.pyc
*.pyo
data/ceol.db
data/*.db
data/*.db.bak*
data/app_info.txt
*.abc
.env


# User data — uploads and SQLite WAL files (local only)
data/uploads/
data/*.db-shm
data/*.db-wal

# macOS
.DS_Store
**/.DS_Store

# Ceòl .gitignore — added 11 May 2026
# These files should never be in the code repository.

# User data: databases, uploads, backups
data/
*.db
*.db-shm
*.db-wal
*.db.bak*
*.db.preflight

# Patch backup files (created by patch scripts)
*.bak-*

# Tombstone files (renamed during cleanup)
*.removed-*

# Python / system
__pycache__/
*.pyc
.DS_Store

# Editor / IDE
.vscode/
.idea/
*.swp

# Environment / secrets
.env
.env.local
